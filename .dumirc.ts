import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base: '/artistry-ui/',
  publicPath: '/artistry-ui/',
  themeConfig: {
    name: 'Artistry-UI',
    socialLinks: {
      github: 'https://github.com/For-lizhuo/artistry-ui',
    },
  },
});
